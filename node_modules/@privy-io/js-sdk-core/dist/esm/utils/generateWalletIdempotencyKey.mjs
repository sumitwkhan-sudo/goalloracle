async function t(t,e){let r=`${t}-auto-${"ethereum"===e?"eth":"sol"}`,n=(new TextEncoder).encode(r);return Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",n))).map((t=>t.toString(16).padStart(2,"0"))).join("")}export{t as generateWalletIdempotencyKey};
