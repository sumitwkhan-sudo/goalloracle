"use strict";exports.generateWalletIdempotencyKey=async function(e,t){let r=`${e}-auto-${"ethereum"===t?"eth":"sol"}`,n=(new TextEncoder).encode(r);return Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",n))).map((e=>e.toString(16).padStart(2,"0"))).join("")};
